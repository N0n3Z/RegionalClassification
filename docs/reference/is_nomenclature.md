# Test whether an object is a nomenclature

Test whether an object is a nomenclature

## Usage

``` r
is_nomenclature(x)
```

## Arguments

- x:

  Any object.

## Value

`TRUE` if `x` is a `nomenclature`, else `FALSE`.
