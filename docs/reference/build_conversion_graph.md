# Build conversion graph from CONVERSION_GRAPH_EDGES

Returns the cached graph after the first call (subsequent calls are
O(1)).

## Usage

``` r
build_conversion_graph()
```

## Value

list (adjacency list) where each element is a list of edges
