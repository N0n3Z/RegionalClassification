# Generate a full conversion feasibility matrix

Generate a full conversion feasibility matrix

## Usage

``` r
get_conversion_matrix()
```

## Value

data.table with from, to, is_simple, relation_chain
