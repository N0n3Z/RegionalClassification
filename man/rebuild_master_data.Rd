% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08_load_prebuilt.R
\name{rebuild_master_data}
\alias{rebuild_master_data}
\title{Rebuild the master table from raw source files and save as RDS}
\usage{
rebuild_master_data(
  raw_dir = get_raw_data_path(),
  out_dir = get_processed_data_path()
)
}
\arguments{
\item{raw_dir}{Path to the raw data directory. Defaults to data/raw/.}

\item{out_dir}{Path where RDS files will be written.
Defaults to inst/extdata/ (accessible after package install).}
}
\value{
master_data list (same as load_master_data() output), invisibly
}
\description{
Call this only when the source files change. Requires all raw XLSX/XLS/CSV
files in data/raw/ to be present, as well as the readxl package.
}
\examples{
\donttest{
  # Only needed when raw source files in data/raw/ change
  master_data <- rebuild_master_data()
}
}
