% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07_dataset_convert.R
\name{convert_dataset}
\alias{convert_dataset}
\title{Convert a geographic code column in a dataset}
\usage{
convert_dataset(
  dt,
  code_col,
  to,
  master_data,
  from = NULL,
  target_col = NULL,
  keep_code = TRUE,
  verbose = TRUE,
  allow_ambiguous = FALSE,
  na_action = c("warn", "keep", "drop")
)
}
\arguments{
\item{dt}{data.table (or data.frame, coerced automatically)}

\item{code_col}{Name of the column containing source codes}

\item{to}{Target classification (e.g. "NUTS_DISTRICT_2021")}

\item{master_data}{Output from build_master_table()}

\item{from}{Source classification. NULL = auto-detect from values.}

\item{target_col}{Name of the new column to create. NULL = auto-generated.}

\item{keep_code}{Keep the original code_col? Default TRUE.}

\item{verbose}{Print conversion path info? Default TRUE.}

\item{allow_ambiguous}{Allow M:N conversions? Default FALSE.
Use split_ambiguous() for weighted M:N handling.}

\item{na_action}{"warn" (default): warn on unmatched codes and keep NAs;
"keep": silently keep NAs;
"drop": remove rows with unmatched codes.}
}
\value{
data.table with target_col added after code_col.
  For M:N conversions, may return more rows than input.
}
\description{
High-level wrapper around convert_codes() that operates directly on a
data.table. Optionally auto-detects the source classification by matching
the column values against known code sets in master_data.
}
\examples{
\donttest{
  master_data <- load_master_data()
  salaries <- data.table::data.table(
    commune = c(21004L, 11002L, 44021L), avg_salary = c(3200, 2900, 2700))

  # Explicit source
  convert_dataset(salaries, "commune", "NUTS_DISTRICT_2021", master_data,
                  from = "NIS_MUNICIPALITY_2019")

  # Auto-detect source
  convert_dataset(salaries, "commune", "NUTS_DISTRICT_2021", master_data)
}
}
