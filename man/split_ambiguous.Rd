% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07_split_ambiguous.R
\name{split_ambiguous}
\alias{split_ambiguous}
\title{Handle M:N conversions on a dataset using weighted splits}
\usage{
split_ambiguous(
  dt,
  code_col,
  value_cols,
  from,
  to,
  master_data,
  weights = NULL,
  value_type = c("additive", "ratio"),
  target_col = NULL,
  normalize = TRUE,
  add_weight_col = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{dt}{data.table with data to convert}

\item{code_col}{Name of the column containing source codes}

\item{value_cols}{Character vector of column names to redistribute.
For value_type="additive": multiplied by weight (totals, counts, sums).
For value_type="ratio": kept unchanged (rates, averages, indices).}

\item{from}{Source classification}

\item{to}{Target classification}

\item{master_data}{Output from build_master_table()}

\item{weights}{How to weight the split. One of:
- NULL (default): use registry if available, otherwise equal weights
- data.table with columns (code_from, code_to, weight)
- character: name of a registered variable ("population", "employment", ...)}

\item{value_type}{"additive" or "ratio". See value_cols.}

\item{target_col}{Name of the new target code column. NULL = auto-generated.}

\item{normalize}{Normalize weights to sum to 1 per code_from? Default TRUE.}

\item{add_weight_col}{Add a 'split_weight' column to the result? Default FALSE.}

\item{verbose}{Print split summary? Default TRUE.}
}
\value{
data.table. Ambiguous codes produce multiple rows; unambiguous codes
  produce one row each.
}
\description{
When converting between classifications where some codes map to multiple
targets (e.g. arrondissement Verviers 63000 -> NUTS3 BE335 + BE336), this
function distributes value columns proportionally using supplied or
registered weights.
}
\examples{
\dontrun{
  arr_salaries <- data.table(
    arr_code   = c(11000L, 62000L, 63000L),   # 63000 = Verviers (ambiguous)
    total_wage = c(5e9, 3e9, 1e9),
    avg_salary = c(2900, 2700, 2400)
  )

  # Equal weights (default)
  split_ambiguous(arr_salaries, "arr_code",
                  value_cols = c("total_wage", "avg_salary"),
                  from = "NIS_ARRONDISSEMENT_2019", to = "NUTS3_2021",
                  master_data,
                  value_type = "additive")

  # Manual population weights for Verviers
  weights <- data.table(
    code_from = c(63000L, 63000L),
    code_to   = c("BE335", "BE336"),
    weight    = c(0.857, 0.143)
  )
  split_ambiguous(arr_salaries, "arr_code",
                  value_cols = "total_wage",
                  from = "NIS_ARRONDISSEMENT_2019", to = "NUTS3_2021",
                  master_data,
                  weights    = weights,
                  value_type = "additive")
}
}
