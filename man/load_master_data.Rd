% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08_load_prebuilt.R
\name{load_master_data}
\alias{load_master_data}
\title{Load the pre-built master table from bundled RDS files}
\usage{
load_master_data(dir = .get_prebuilt_dir())
}
\arguments{
\item{dir}{Path to the directory containing the RDS files.
Defaults to the package extdata directory.}
}
\value{
Named list identical in structure to the output of build_master_table().
        The four intermediate hierarchy lists are set to NULL as they are not
        needed at runtime.
}
\description{
This is the default and fast way to initialise the package.
No raw source files are required.  All R types (integer, character, etc.)
are preserved exactly as serialised by save_master_tables().
}
\examples{
\donttest{
  master_data <- load_master_data()
  # master_data now contains all lookup tables, ready for convert_codes() etc.
}
}
